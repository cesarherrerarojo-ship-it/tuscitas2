# TuCitaSegura — Entorno de Producción

# App Check / reCAPTCHA
# En producción NO usar fail-open; configurar el secreto real de reCAPTCHA v3.
RECAPTCHA_FAILOPEN_DEV=false
RECAPTCHA_SECRET=TU_SECRETO_RECAPTCHA_V3

# API Keys
GMAPS_API_KEY=TU_API_KEY_DE_GOOGLE_MAPS

# CORS / Origen permitido (ajusta a tu dominio)
ALLOWED_ORIGIN=https://tucitasegura.com

# Política de membresía (backend)
# Hombres requieren membresía por defecto (true). Mujeres opcional (false por defecto).
REQUIRE_MEMBERSHIP_MALE=true
REQUIRE_MEMBERSHIP_FEMALE=false

# Política de depósito antidefault (en EUR)
INSURANCE_DEPOSIT_MIN=120

# Notas de despliegue:
# - Verifica que App Check esté “Enforced” cuando el site key emita tokens correctamente.
# - Asegura que tu site key de reCAPTCHA incluya tus dominios (Hosting y propios).
# - Ajusta la CSP de producción para permitir https://www.google.com y https://www.gstatic.com.
