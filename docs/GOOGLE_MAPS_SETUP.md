# Setup Google Maps

Backend:
- Endpoint: `GET /api/maps-key`.
- Variable: `GMAPS_API_KEY`.

Recomendaciones:
- Restringir clave por dominio y por API.
- Mantener la clave fuera del cliente cuando sea posible.

